import React from 'react';
import { CreateCollectionModal } from '@widgets/create-collection-modal';

const Page = () => {
  return <CreateCollectionModal collectionType="custom" />;
};

export default Page;
