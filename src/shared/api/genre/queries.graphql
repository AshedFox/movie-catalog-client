query GetGenres(
  $limit: Int!
  $offset: Int!
  $filter: GenreFilter
  $sort: GenreSort
) {
  getGenres(limit: $limit, offset: $offset, filter: $filter, sort: $sort) {
    nodes {
      id
      name
    }
  }
}
