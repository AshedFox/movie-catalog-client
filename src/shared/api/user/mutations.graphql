mutation UpdateAvatar($file: Upload!) {
  updateAvatar(file: $file) {
    id
    name
    email
    createdAt
    avatar {
      url
    }
    country {
      id
      name
    }
    role
    isEmailConfirmed
  }
}
