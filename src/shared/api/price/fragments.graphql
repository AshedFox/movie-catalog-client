fragment BasePrice on Price {
  id
  amount
  currencyId
}

fragment PlanPrice on Price {
  id
  amount
  interval
  currency {
    id
    name
    symbol
  }
}

fragment Price on Price {
  id
  amount
  currency {
    id
    name
    symbol
  }
}
